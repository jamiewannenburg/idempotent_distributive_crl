if(Mace4).   % Options for Mace4
  assign(max_seconds, -1).
  assign(max_models, -1).
end_if.

formulas(assumptions).

x<=x.
(x<=y & y<=z) -> x<=z.
(x<=y & y<=x) -> x=y.

x*(y*z)=(x*y)*z.
x*y = y*x.
e*x=x.

x<=y -> z*x <= z*y.

(x*y<=z)<->(y<=x\z).

x=x*x.

exists x (-(e <= x) & all y (e<=y | y<=x)).

end_of_list.

formulas(goals).

end_of_list.
